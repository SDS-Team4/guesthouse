package com.guesthouse.shared.domain.reservation;

public enum ReservationActionType {
    REQUESTED,
    HOST_CONFIRMED,
    HOST_REJECTED,
    GUEST_CANCELLED,
    HOST_CANCELLED,
    ADMIN_CANCELLED
}
