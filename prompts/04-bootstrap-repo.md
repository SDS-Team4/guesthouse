Use AGENTS.md, PLANS.md, and the architecture notes as source of truth.

Implement only milestone M0 bootstrap.

Tasks:
1. create the repository/module skeleton,
2. create placeholder README files in each top-level app directory,
3. add shared coding conventions/config where safe,
4. add empty test directories or starter test scaffolds,
5. add docs placeholders needed for later milestones,
6. do not implement business features yet.

Rules:
- Keep diffs small.
- Do not guess frameworks that are not yet decided.
- If a build tool is undecided, create a documented placeholder instead of hardcoding one.
- Update docs/plan/STATUS.md.

At the end:
- summarize created structure,
- list unresolved decisions still blocking M1.
